/** The Operator–nested playbook URLs (single source for links and redirects). */
export const OPERATOR_BASE = '/trader-deck/trade-validator';
/** @deprecated Use OPERATOR_BASE */
export const TRADE_VALIDATOR_BASE = OPERATOR_BASE;
export const PLAYBOOK_HUB_PATH = `${OPERATOR_BASE}/trader-playbook`;
export const PLAYBOOK_MISSED_REVIEW_PATH = `${PLAYBOOK_HUB_PATH}/missed-review`;
