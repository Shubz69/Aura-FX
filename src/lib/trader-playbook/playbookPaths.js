/** Trade Validator–nested playbook URLs (single source for links and redirects). */
export const TRADE_VALIDATOR_BASE = '/trader-deck/trade-validator';
export const PLAYBOOK_HUB_PATH = `${TRADE_VALIDATOR_BASE}/trader-playbook`;
export const PLAYBOOK_MISSED_REVIEW_PATH = `${PLAYBOOK_HUB_PATH}/missed-review`;
